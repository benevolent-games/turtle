
import {nap} from "../utils/nap.js"

// we can do async work, like reading files, etc
await nap(0)

export const description = "slow and steady static site generator"
export const random_value = Math.random()

